Fix `~xrayvision.clean.ms_clean` producing incorrect flux when run with more than one scale. The cross-terms used to update the residual at each scale were being computed before all the scale kernels existed. The scale kernels are also now normalised to unit volume rather than unit peak so that the flux found at different scales is directly comparable, matching the reference CASA ``LatticeCleaner`` implementation.
